# Runtime dependencies for Part 1 (Tokenization).
# Dev tooling (ruff, pytest, pre-commit) lives in requirements-dev.txt.
# Additional deps (torch, matplotlib, etc.) will be added when later
# parts need them.

numpy>=1.24.0
jupyter>=1.0.0
tiktoken>=0.4.0
tqdm>=4.65.0
